#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int card_umber = get_long("Number: \n");
    while (card_number < 0)
    {
        card_number = get_long("Number: \n");
    }

}

int check_card_type(number)
{
    int first
}